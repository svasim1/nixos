#!/usr/bin/env bash
set -euo pipefail

AGENIX_DIR="$HOME/.config/agenix"
KEY_FILE="$AGENIX_DIR/key"

echo "Paste your PRIVATE key (including BEGIN and END lines), then press Ctrl+D:"
mkdir -p "$AGENIX_DIR"
cat > "$KEY_FILE"
chmod 600 "$KEY_FILE"

echo "Agenix identity key imported to $KEY_FILE"
